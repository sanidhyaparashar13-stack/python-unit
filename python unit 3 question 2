def convert(c):
    return (c * 9/5) + 32
  def convert(f):
    return (f - 32) * 5/9
    def convert(c):
    return c + 273.15from celsius_to_fahrenheit import convert as ctof
from fahrenheit_to_celsius import convert as ftoc
from celsius_to_kelvin import convert as ctok

c = float(input("Enter Celsius: "))
print("Fahrenheit:", ctof(c))
print("Kelvin:", ctok(c))

f = float(input("Enter Fahrenheit: "))
print("Celsius:", ftoc(f))
